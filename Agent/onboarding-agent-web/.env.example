# API (include /api/v1). Upload images are served from the same host at /uploads/...
VITE_API_BASE_URL=http://localhost:5000/api/v1

# Optional: override origin for /uploads only (defaults to origin of VITE_API_BASE_URL)
# VITE_API_HOST=http://localhost:5000
